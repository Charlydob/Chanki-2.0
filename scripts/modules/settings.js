import { authMock } from '../auth.mock.js';

const isIos = () => /iPad|iPhone|iPod/.test(navigator.userAgent);

export const renderSettings=(root,go,diag,hardReset)=>{const user=authMock.user();const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;root.innerHTML=`<div class="view-grid"><article class="card view-grid"><p>Usuario: ${user?.email||'mock@local'}</p><p>Idioma natal: Español</p><p>Idioma objetivo: Alemán</p><p class="small">standalone: ${String(standalone)} · navigator.standalone: ${String(window.navigator.standalone===true)}</p>${!standalone?'<p class="status-warn">Estás en navegador, abre desde el icono de pantalla de inicio.</p>':''}<div class="row wrap"><button id="logout" class="btn-ghost">Cerrar sesión</button><button id="reset" class="btn-ghost">Hard reset</button><button id="copy" class="btn">Copiar diagnóstico</button></div></article></div>`; root.querySelector('#logout').onclick=()=>{authMock.logout();go('login');}; root.querySelector('#reset').onclick=hardReset; root.querySelector('#copy').onclick=diag;};
